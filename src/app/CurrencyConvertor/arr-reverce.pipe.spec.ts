import { ArrRevercePipe } from './arr-reverce.pipe';

describe('ArrRevercePipe', () => {
  it('create an instance', () => {
    const pipe = new ArrRevercePipe();
    expect(pipe).toBeTruthy();
  });
});
